name: anki_flutter
description: Open-source Flutter frontend for Anki.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.13.0 <4.0.0'
  flutter: 3.47.5

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.2.0
  protobuf: ^6.1.0
  fixnum: ^1.1.1
  file_picker: ^13.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
